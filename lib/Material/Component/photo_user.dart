import 'package:flutter/material.dart';

class PhotoUser extends StatelessWidget {
  const PhotoUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
